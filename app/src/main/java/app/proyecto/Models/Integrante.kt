package app.proyecto.Models

import android.widget.CheckBox

class Integrante(nombre:String,totalParcial:Double){
        var nombre=nombre
        var totalParcial=totalParcial
        lateinit var chek:CheckBox
}